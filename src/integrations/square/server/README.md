# Backend Template Note

This file is a placeholder for your real backend project (Node, Bun, Deno, etc).

Move `createPaymentLink.ts` logic to your backend service and inject:
- `SQUARE_ACCESS_TOKEN`
- `SQUARE_ENVIRONMENT`
- webhook verification settings as needed

Do not deploy secret handling inside frontend code.
